## What is this

Sets are used to store multiple items in a single variable.

A set is a collection which is _unordered_, _unchangeable_, _unindexed_ and do not allow duplicate values.

## Frozenset

```python
fs = frozenset([1, 2, 3]) 
d = {fs: "значение"} # работает
```
